package com.samim.jarvis.voice

import android.content.Context
import android.util.Log

// Placeholder: integrate Google Speech-to-Text or local engine like VOSK
class SpeechToTextManager(private val context: Context) {
    fun startListening() {
        Log.d("STT", "startListening placeholder")
    }

    fun stopListening() {
        Log.d("STT", "stopListening placeholder")
    }
}
