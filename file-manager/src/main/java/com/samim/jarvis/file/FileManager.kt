package com.samim.jarvis.file

import android.content.Context

class FileManager(private val context: Context) {
    fun listFiles(path: String): List<String> {
        // Lightweight placeholder for listing files
        return emptyList()
    }
}
